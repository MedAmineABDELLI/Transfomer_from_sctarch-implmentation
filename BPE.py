

cratcters = {'h' , 'u' , 'g' , 'p' , 'n' , 's' , '$'}
corpus = [
    "hug$"
    "pug$"
    "pun$"
    "hugs$"
    ]

def caracter_count(corpus):
    tokens = {

    }
    for i in range(len(corpus)):
        word = corpus[i]
        for j in word:
            chars = word.split("")
            for k in chars:
                token = chars[k] + chars[k+1]
                tokens{
                    "caracter" : token,
                    "redundancy":
                }

            
